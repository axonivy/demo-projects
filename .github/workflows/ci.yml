name: CI-Build

on:
  push:
  schedule:
    - cron:  '21 21 * * *'
  workflow_dispatch:

jobs:
  build:
    uses: axonivy-market/github-workflows/.github/workflows/ci.yml@v5
    with:
      mvnArgs: -Divy.engine.version.latest.minor=true -Dmaven.test.skip=true # We run the tests on our Jenkins CI
