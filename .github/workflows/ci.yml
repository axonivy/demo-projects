name: CI-Build

permissions:
  contents: read
  checks: write
  pull-requests: write

on:
  push:
  schedule:
    - cron:  '21 21 * * *'
  workflow_dispatch:

jobs:
  build:
    uses: axonivy-market/github-workflows/.github/workflows/dev.yml@v6
    with:
      ivyPluginVersion: 13.2.0-SNAPSHOT
      ivyVersion: nightly-13.2
      mvnArgs: -Dmaven.test.skip=true # We run the tests on our Jenkins CI
